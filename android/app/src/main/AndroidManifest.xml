<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Keeps audio alive when the app is backgrounded or the screen is off. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Playback controls notification (Android 13+ asks at runtime). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Saving a download into the gallery. Scoped storage handles this from
         API 29, so the permission is capped rather than requested forever. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- The floating player: a window above other apps. Android never grants
         this through a runtime permission dialog - declaring it only earns the
         right to send the user to a settings screen, which
         FloatingPlayer.requestPermission does. It also happens to be the
         documented exemption that lets the bubble start the app from the
         background when it is tapped. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Auto Backup keeps the library (database + settings) in the user's
         Google backup so a reinstall restores subscriptions, history and
         playlists. The rules files exclude the downloaded videos - included,
         they exceed the backup quota and Android skips the backup entirely. -->
    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Holds the floating player's overlay window. A foreground service
             because a view added to the WindowManager dies with the context
             that added it, and the Activity is exactly what has gone away when
             the bubble is supposed to be visible.

             foregroundServiceType is mandatory from Android 14. mediaPlayback
             rather than specialUse: the bubble only exists while this app is
             playing, and FOREGROUND_SERVICE_MEDIA_PLAYBACK is already declared
             above for the player's own notification, whereas specialUse would
             need an extra property and a Play review this app will never
             have. exported=false - nothing outside the app may start it. -->
        <service
            android:name=".FloatingPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open the update download link in a browser (Android 11+
             requires declaring the intent to resolve it). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
